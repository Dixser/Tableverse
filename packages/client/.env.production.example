# Copy to .env.production (git-ignored) or set as a build-time env var in
# your Cloudflare Pages project settings. Required for any deployment where
# the client is served from a different origin than the backend -- see
# packages/client/src/config.ts and the root docker-compose.yml/Caddyfile.
VITE_API_BASE_URL=https://123-45-67-89.nip.io
